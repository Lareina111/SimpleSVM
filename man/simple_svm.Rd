% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple_svm.R
\name{simple_svm}
\alias{simple_svm}
\title{Simple SVM for Binary Classification}
\usage{
simple_svm(x, y, lambda = 0.01)
}
\arguments{
\item{x}{A matrix or data frame of predictor variables, where each row is an observation.}

\item{y}{A numeric vector representing the binary response variable, with values -1 or 1.}

\item{lambda}{A numeric value representing the regularization parameter. Defaults to 0.01.}
}
\value{
A list containing the following elements:
\item{weights}{The weights vector of the model.}
\item{intercept}{The intercept (bias term) of the model.}
}
\description{
This function trains a simple support vector machine (SVM) for binary classification
using gradient descent. It aims to classify data points with labels -1 and 1.
}
\examples{
# Generate some sample data
set.seed(123)
x <- matrix(rnorm(20), ncol = 2)
y <- ifelse(x[,1] + x[,2] > 0, 1, -1)

# Train the simple SVM model
model <- simple_svm(x, y)

# Print model weights and intercept
print(model$weights)
print(model$intercept)
}
